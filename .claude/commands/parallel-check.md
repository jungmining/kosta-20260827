---
description: 테스트와 리뷰를 동시에 돌린다.
---


아래 둘을 **한 번에** 실행할 것. 순서를 두지 않는다.


대상: $ARGUMENTS


- **tester** - 대상의 테스트를 만들어 `tests/`에 넣고 `npm test`로 확인한다.
- **reviewer** - 대상을 CLAUDE.md 규약 기준으로 리뷰한다.


둘 다 끝나면 결과를 **하나의 표**로 모아준다.
두 결과가 서로 모순되면 **모순이라고 적고 어느 쪽으로 선택했는지 근거를 작성한다.**
